class CryptPuzzleSolver():
    
    #CSP problem
    #X = {distinct letters in equation}
    #D of each variable = {0-9}
    #C {right most letters cannot be 0 if they equal a different value,
    #   one of right most letters cannot be 0 if they equal a value different than both of them
    #   left most digit in result must be 1 if greater than both added numbers,
    #   no leading zeros,
    #   no letter can have the same value}
    def solve():
        #start with D everything is 0
        # get first two letters
        # change values until the first constraint matches
    
